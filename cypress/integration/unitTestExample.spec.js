import { Paths } from '../../src/constants/Paths.js'

describe('Example of a unit test', () => {
    it('is working', () => {
        expect(Paths['API_URL']).to.equal('https://gb.ryholt.xyz')
    })
}) 